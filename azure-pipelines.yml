pool:
  vmImage: 'windows-latest'
   
steps:

- task: Docker@2
  inputs:
    containerRegistry: 'docker'
    command: 'build'
    Dockerfile: '**/Dockerfile'
    tags: 'infraasapi:latest'
- task: Docker@2
  inputs:
    containerRegistry: 'docker'
    repository: 'infraasapi'
    command: 'push'
    tags: 'infraasapi:latest'
